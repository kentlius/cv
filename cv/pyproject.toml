[project]
name = "cv"
version = "0.1.0"
description = "Kent Liusudarso's CV: YAML source + rendercv pipeline"
requires-python = ">=3.12"
readme = "README.md"

dependencies = [
    "rendercv[full]==2.8",
]
